# This file ensures packages under this directory can be imported by any other package here.
from setuptools import setup, find_packages

setup(name='myproject', version='1.0', packages=find_packages())
